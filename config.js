/*
 * Public browser configuration.
 * Replace YOUR_PROJECT_REF with the project reference from your Supabase URL.
 * No service-role key, database password, or other secret belongs in this file.
 */
window.AKRAPEX_CONFIG = Object.freeze({
  supabaseUrl: "https://jswqbrbeduurtzavdfce.supabase.co",
  functionName: "join-waitlist",

  // Use the final public page URL so every generated referral link is shareable.
  // Leave this empty during local development to use the current page URL.
  siteUrl: "https://akrapex.github.io/akrapex-waitlist/",
});

// /*
//  * Public browser configuration.
//  * Replace YOUR_PROJECT_REF with the project reference from your Supabase URL.
//  * No service-role key, database password, or other secret belongs in this file.
//  */
// window.AKRAPEX_CONFIG = Object.freeze({
//   supabaseUrl: "https://jswqbrbeduurtzavdfce.supabase.co",
//   functionName: "join-waitlist",

//   // Use the final public page URL so every generated referral link is shareable.
//   // Leave this empty during local development to use the current page URL.
//   siteUrl: "https://akrapex.github.io/akrapex-waitlist/",
// });
